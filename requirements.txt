fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
jinja2==3.1.5
python-dotenv==1.0.1

# Scanner toolchain. Pinned because the finding set is version-dependent:
# bandit 1.8.x does not implement B704 (markupsafe XSS), so an unpinned install
# silently produces a different backlog than the one documented in the README.
bandit==1.9.4
